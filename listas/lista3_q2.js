/**
 * Q2. Faça um algoritmo para imprimir False ao comparar se o número A é maior que o número B.
 */


const prompt = require('prompt-sync')({sigint: false});

numeroA = 10
numeroB = 20

console.log(numeroA > numeroB)